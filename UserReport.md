# تقارير المستخدم - User Reports

هذا الملف يحتوي على سجل بجميع التقارير والتعديلات التي تم تنفيذها بناءً على طلب المستخدم.

---

## 📅 التقرير الأول: 2026-02-11

### 🐛 إصلاح مشكلة زر الرجوع في الخطوات الفرعية

#### 📋 ملخص المشكلة
في "البدء البسيط"، عندما كان المستخدم يضيف بيانات متكررة (مثل مدرستين أو خبرتين عمل)، وقرر الرجوع لتعديل أو إلغاء المدخل الثاني، كان زر "رجوع" لا يعمل بشكل صحيح.

#### 🔍 السبب الجذري
**الملف:** [`src/components/wizard/QuestionnaireStep.tsx`](src/components/wizard/QuestionnaireStep.tsx)

1. **الـ history كان يخزن اسم الحقل فقط** دون تحديد أي مدخل (entry index):
   ```typescript
   // الكود القديم
   const [questionHistory, setQuestionHistory] = useState<string[]>([]);
   ```

2. **عند الرجوع، كان يفترض أن الحقل ينتمي للمدخل الأخير دائماً**:
   ```typescript
   const idx = list.length - 1;  // ⚠️ دائماً آخر مدخل!
   ```

3. **عند الرجوع من `education_more`، لم يكن يحذف المدخل الفارغ** الذي أُنشئ حديثاً.

#### ✅ الحل المطبق

1. **تحديث بنية الـ history** لتخزين `entryIndex`:
   ```typescript
   interface HistoryEntry {
       field: string;
       entryIndex?: number; // لتتبع أي مدخل في المصفوفة
   }
   const [questionHistory, setQuestionHistory] = useState<HistoryEntry[]>([]);
   ```

2. **تحديث `handleAnswer`** لحفظ `entryIndex` مع اسم الحقل للحقول المتكررة (education, experience, languages).

3. **تحديث `handleInternalBack`** لاستخدام `entryIndex` لتحديد المدخل الصحيح عند مسح البيانات.

4. **إضافة منطق حذف المدخلات الفارغة** عند الرجوع من `education_more`, `experience_more`, `languages_more`.

#### 📊 النتيجة
- TypeScript compilation: **passed** ✅
- الآن يمكن للمستخدم الرجوع بشكل صحيح بين المدخلات المتعددة

#### 📁 الملفات المعدلة
- [`src/components/wizard/QuestionnaireStep.tsx`](src/components/wizard/QuestionnaireStep.tsx)

---
