# تقارير المستخدم - User Reports

هذا الملف يحتوي على سجل بجميع التقارير والتعديلات التي تم تنفيذها بناءً على طلب المستخدم.

---

## 📅 التقرير السادس: 2026-02-12

### 🐛 إصلاح مشاكل متعددة في التنقل والبناء

#### 📋 ملخص المشاكل

1. **مشكلة زر الرجوع في خطوة رفع الصورة:**
   - عند الضغط على "رجوع" من سؤال الصورة الشخصية، كان يظهر "تم اختيار الصورة بنجاح" رغم عدم وجود صورة.
   - السبب: كان `response` يحتوي على قيمة `__skipped__` أو قيمة قديمة.

2. **مشكلة زر الرجوع في خطوة معلومات التواصل:**
   - عند الضغط على "رجوع" من ContactStep، كان يرجع للصفحة الرئيسية (وضع الاختيار) بدلاً من خطوة إدخال الاسم.
   - السبب: WelcomeStep كان يبدأ دائماً بوضع 'select' ولا يتذكر أن المستخدم كان في وضع 'manual'.

3. **مشكلة زر الرجوع في خطوة التعليم:**
   - عند الرجوع لحقول التعليم، كانت البيانات تُمسح بدلاً من إبقائها للتعديل.
   - السبب: كان الكود يمسح البيانات عند الرجوع ولا يعيد تعبئة `response` بالقيمة السابقة.

4. **تحذيرات Edge Runtime في ملف PDF route:**
   - ظهور تحذيرات أثناء البناء على Cloudflare بسبب استخدام وحدات Node.js غير مدعومة.

#### ✅ الحلول المطبقة

1. **إصلاح زر الرجوع في خطوة الصورة:**
   ```typescript
   // في QuestionnaireStep.tsx - handleInternalBack
   } else if (lastField === 'photoUrl') {
       // لا نعيد تعيين response بقيمة قديمة - نريد عرض واجهة رفع الصورة فارغة
       setResponse('');
   }
   ```

2. **إصلاح زر الرجوع في ContactStep:**
   ```typescript
   // في WelcomeStep.tsx
   const [mode, setMode] = useState<QuickStartMode>(
       data.personal.firstName || data.personal.lastName ? 'manual' : 'select'
   );
   ```

3. **إصلاح زر الرجوع في التعليم:**
   ```typescript
   // في QuestionnaireStep.tsx - handleInternalBack
   // Pre-populate response with current value for array fields BEFORE clearing
   if (lastField.startsWith('education_') && entryIndex !== undefined) {
       const list = data.education || [];
       const idx = entryIndex;
       if (idx >= 0 && idx < list.length) {
           const edu = list[idx];
           if (lastField === 'education_institution') setResponse(edu.institution || '');
           // ... باقي الحقول
       }
       // Don't clear data - keep it for editing
   }
   ```

4. **تحسين معالجة Edge Runtime في PDF route:**
   - إضافة تحقق من وجود Node.js runtime قبل استدعاء وحدات Node.js
   - استخدام `require` ديناميكي مع `eslint-disable` لتجنب التحذيرات

#### 📊 النتيجة
- Build: **passed** ✅
- زر الرجوع يعمل بشكل صحيح في جميع الخطوات
- البيانات تُحفظ عند الرجوع للتعديل
- التحذيرات تم تقليلها بشكل كبير

#### 📁 الملفات المعدلة
- [`src/components/wizard/QuestionnaireStep.tsx`](src/components/wizard/QuestionnaireStep.tsx) - إصلاح منطق الرجوع
- [`src/components/wizard/WelcomeStep.tsx`](src/components/wizard/WelcomeStep.tsx) - تذكر وضع الإدخال اليدوي
- [`src/app/api/analyze/pdf/route.ts`](src/app/api/analyze/pdf/route.ts) - تحسين معالجة Edge Runtime

---

## 📅 التقرير الخامس: 2026-02-12

### 🚀 إصلاح مشاكل البناء والنشر على Cloudflare (Windows Fix)

#### 📋 ملخص المشكلة
عند محاولة نشر المشروع على Cloudflare Pages باستخدام نظام Windows، ظهرت عدة مشاكل:
1. **Edge Runtime:** فشل البناء لبعض الواجهات (APIs) والصفحات لأنها لم تكن مفعلة لاستخدام الـ Edge Runtime المطلوب من Cloudflare.
2. **Lambda lookup error:** فشل في تحديد الـ Lambdas لبعض المسارات مثل `/panel/sessions`.
3. **spawn npx ENOENT:** خطأ في تنفيذ سكريبت البناء التلقائي لـ Cloudflare بسبب مشكلة في استدعاء `npx` داخل بيئة Windows.

#### ✅ الحل المطبق

1. **تفعيل Edge Runtime في جميع المسارات:**
   - تم إضافة `export const runtime = 'edge';` لجميع الـ API Routes في `src/app/api`.
   - تم إضافة نفس التصريح لجميع صفحات لوحة التحكم في `src/app/panel`.
   - تم إضافة التصريح للصفحة الرئيسية `src/app/page.tsx`.

2. **تحديث ملف `package.json` بمعالج بناء ذكي:**
   - تم إضافة `vercel` كـ `devDependency` لضمان وجوده محلياً.
   - تم تعديل سكريبت `pages:build` ليكون: `"vercel build && next-on-pages --skip-build"`.
   - هذا التعديل يحل مشكلة `ENOENT` لأنه يستدعي البناء يدوياً ثم يطلب من أداة Cloudflare معالجة النتائج الجاهزة.

#### 📊 النتيجة
- Build: **passed** ✅
- Deployment compatibility: **verified** ✅
- الآن يعمل سكريبت `npm run deploy` بشكل صحيح وسلس على أنظمة Windows.

#### 📁 الملفات المعدلة
- `package.json` - تحديث السكريبتات وإضافة التبعيات.
- `src/app/api/**/*.ts` - تفعيل Edge Runtime.
- `src/app/panel/**/*.tsx` - تفعيل Edge Runtime.
- `src/app/page.tsx` - تفعيل Edge Runtime.

---

## 📅 التقرير الرابع: 2026-02-11

### 🚀 إنشاء سكريبت النشر التلقائي (Deploy Script)

#### 📋 ملخص الطلب
إنشاء ملف سكريبت (PowerShell أو Batch) للقيام بعملية deploy للمشروع عبر git مع:
- آلية لإدخال commit مناسب
- عمل push للمشروع على git
- تشغيل المشروع ونشره على Cloudflare

#### ✅ الحل المطبق

تم إنشاء ملفين:

1. **[`deploy.ps1`](deploy.ps1)** - سكريبت PowerShell الرئيسي:
   - عرض حالة Git الحالية
   - عرض الملفات المعدلة والجديدة
   - اقتراح رسائل commit مناسبة بناءً على التغييرات
   - خيار لكتابة رسالة مخصصة
   - إضافة الملفات وعمل commit
   - رفع التغييرات إلى GitHub (push)
   - بناء المشروع ونشره على Cloudflare

2. **[`deploy.bat`](deploy.bat)** - ملف Batch لتشغيل السكريبت بسهولة:
   - يعمل كـ wrapper للسكريبت PowerShell
   - يدعم تمرير المعاملات

#### 🎯 خيارات الاستخدام

```powershell
# تشغيل تفاعلي (يسأل عن رسالة commit)
.\deploy.ps1

# تحديد رسالة commit مباشرة
.\deploy.ps1 -Message "إصلاح مشكلة التنقل"

# تخطي عملية deploy (فقط commit و push)
.\deploy.ps1 -SkipDeploy

# تخطي عملية push (فقط commit)
.\deploy.ps1 -SkipPush

# عرض المساعدة
.\deploy.ps1 -Help
```

#### 📊 النتيجة
- تم إنشاء سكريبت سهل الاستخدام للنشر
- يدعم اقتراحات ذكية لرسائل commit
- يمكن تخصيص العملية حسب الحاجة

#### 📁 الملفات المُنشأة
- [`deploy.ps1`](deploy.ps1) - سكريبت PowerShell الرئيسي
- [`deploy.bat`](deploy.bat) - ملف Batch للتشغيل السهل

---

## 📅 التقرير الثالث: 2026-02-11

### 🐛 إصلاح مشكلة زر الرجوع في التنقل الداخلي للأسئلة

#### 📋 ملخص المشكلة
في "البدء البسيط"، بعد إدخال الاسم والكنية والانتقال لرقم الهاتف ثم تاريخ الميلاد، عند الضغط على زر "رجوع" كان يرجع لرقم الهاتف بدلاً من البقاء في نفس السؤال أو العودة للسؤال السابق المنطقي.

**نفس المشكلة كانت تظهر في:**
- تاريخ الميلاد (birthDate)
- المسمى الوظيفي (targetJobTitle)
- البريد الإلكتروني (email)
- اختيار الصورة الشخصية (photoUrl)
- ضمن التعليم: زر الرجوع يذهب دائماً لرقم الهاتف

#### 🔍 السبب الجذري
**الملف:** [`src/components/wizard/QuestionnaireStep.tsx`](src/components/wizard/QuestionnaireStep.tsx)

1. **الـ `questionHistory` كان يبدأ فارغاً** عند الدخول لـ QuestionnaireStep
2. **يتم إضافة السؤال للـ history فقط بعد الإجابة عليه** (في `handleAnswer`)
3. **عند الضغط على "رجوع" من سؤال لم تتم الإجابة عليه بعد**:
   ```typescript
   const handleInternalBack = () => {
       if (questionHistory.length === 0) {
           onBack();  // ⚠️ هذا يرجع لرقم الهاتف!
           return;
       }
       // ...
   };
   ```

#### ✅ الحل المطبق

1. **إضافة دالة `initializeHistoryFromData`** لتهيئة الـ history بناءً على البيانات الموجودة:
   ```typescript
   const initializeHistoryFromData = (cvData: CVData): HistoryEntry[] => {
       const history: HistoryEntry[] = [];
       
       // Personal Info fields
       if (cvData.personal.birthDate && !isSkipped(cvData.personal.birthDate)) {
           history.push({ field: 'birthDate' });
       }
       if (cvData.personal.targetJobTitle) {
           history.push({ field: 'targetJobTitle' });
       }
       // ... باقي الحقول
       
       return history;
   };
   ```

2. **إضافة useEffect لتهيئة الـ history عند تحميل المكون**:
   ```typescript
   useEffect(() => {
       if (!historyInitialized) {
           const initialHistory = initializeHistoryFromData(data);
           if (initialHistory.length > 0) {
               setQuestionHistory(initialHistory);
           }
           setHistoryInitialized(true);
       }
   }, [historyInitialized, data]);
   ```

3. **إضافة دالة مساعدة `isSkipped`** للتحقق من الحقول المتخطاة:
   ```typescript
   const isSkipped = (val: string | undefined | null): boolean => val === '__skipped__';
   ```

#### 📊 النتيجة
- Build: **passed** ✅
- الآن يمكن للمستخدم الرجوع بشكل صحيح بين الأسئلة
- زر الرجوع يعمل كعكس زر التالي بشكل منطقي

#### 📁 الملفات المعدلة
- [`src/components/wizard/QuestionnaireStep.tsx`](src/components/wizard/QuestionnaireStep.tsx) - إضافة تهيئة الـ history

---

## 📅 التقرير الثاني: 2026-02-11

### 🔄 نقل واجهة الدفع إلى مرحلة تصدير PDF

#### 📋 ملخص المشكلة
كان المستخدم يرغب في نقل واجهة الدفع من خطوة منفصلة في الـ wizard إلى مرحلة تصدير ملف PDF. عند الضغط على زر "تصدير PDF"، تظهر نافذة الدفع مع خيارات:
- إذا كان الدفع **إجباري (mandatory)**: يجب الدفع قبل التصدير
- إذا كان الدفع **تبرع (donation)**: يمكن التخطي والدفع أو التصدير مباشرة
- إذا كان الدفع **معطل (disabled)**: التصدير مباشرة بدون دفع

#### 🔍 السبب الجذري
**الملفات المعنية:**
- [`src/app/page.tsx`](src/app/page.tsx) - يحتوي على تعريف الخطوات
- [`src/components/preview/CVPreview.tsx`](src/components/preview/CVPreview.tsx) - يحتوي على منطق التصدير
- [`src/components/payment/ShamCashPayment.tsx`](src/components/payment/ShamCashPayment.tsx) - واجهة الدفع القديمة

**المشكلة:** كانت واجهة الدفع خطوة منفصلة (Step 4) قبل خطوة المعاينة، مما يعني أن المستخدم يجب أن يمر بالدفع قبل رؤية السيرة الذاتية النهائية.

#### ✅ الحل المطبق

1. **إزالة خطوة الدفع من الـ wizard** في [`src/app/page.tsx`](src/app/page.tsx):
   ```typescript
   // قبل التعديل (5 خطوات)
   const steps = [
     { component: WelcomeStep, title: 'Welcome' },
     { component: ContactStep, title: 'Contact' },
     { component: QuestionnaireStep, title: 'Questionnaire' },
     { component: ShamCashPayment, title: 'Payment' },  // ❌ تمت الإزالة
     { component: CVPreview, title: 'Preview' },
   ];

   // بعد التعديل (4 خطوات)
   const steps = [
     { component: WelcomeStep, title: 'Welcome' },
     { component: ContactStep, title: 'Contact' },
     { component: QuestionnaireStep, title: 'Questionnaire' },
     { component: CVPreview, title: 'Preview' },
   ];
   ```

2. **إضافة واجهة الدفع في CVPreview** عند الضغط على تصدير:
   - إضافة حالة `showPaymentModal` للتحكم في عرض نافذة الدفع
   - إضافة حالة `paymentSettings` لجلب إعدادات الدفع من API
   - إضافة حالة `selectedExportOption` لتذكر خيار التصدير المختار

3. **تعديل منطق التصدير**:
   ```typescript
   const handleExport = async (option: 'ar' | 'en' | 'both') => {
     // التحقق من حالة الدفع قبل التصدير
     if (paymentSettings.paymentType !== 'disabled' && data.metadata.paymentStatus !== 'completed') {
       setSelectedExportOption(option);
       setShowPaymentModal(true);
       return;
     }
     await performExport(option);
   };
   ```

4. **إضافة نافذة الدفع Modal** مع:
   - عرض رمز QR للدفع
   - رفع إثبات الدفع
   - زر التخطي (للتبرع الاختياري فقط)
   - معالجة الدفع وتحسين السيرة الذاتية بالذكاء الاصطناعي

#### 📊 النتيجة
- Build: **passed** ✅
- الآن يمر المستخدم بـ 4 خطوات فقط قبل رؤية السيرة الذاتية
- الدفع يظهر فقط عند محاولة التصدير
- إذا كان الدفع اختياري، يمكن للمستخدم التخطي

#### 📁 الملفات المعدلة
- [`src/app/page.tsx`](src/app/page.tsx) - إزالة خطوة الدفع
- [`src/components/preview/CVPreview.tsx`](src/components/preview/CVPreview.tsx) - إضافة واجهة الدفع عند التصدير

---

## 📅 التقرير الأول: 2026-02-11

### 🐛 إصلاح مشكلة زر الرجوع في الخطوات الفرعية

#### 📋 ملخص المشكلة
في "البدء البسيط"، عندما كان المستخدم يضيف بيانات متكررة (مثل مدرستين أو خبرتين عمل)، وقرر الرجوع لتعديل أو إلغاء المدخل الثاني، كان زر "رجوع" لا يعمل بشكل صحيح.

#### 🔍 السبب الجذري
**الملف:** [`src/components/wizard/QuestionnaireStep.tsx`](src/components/wizard/QuestionnaireStep.tsx)

1. **الـ history كان يخزن اسم الحقل فقط** دون تحديد أي مدخل (entry index):
   ```typescript
   // الكود القديم
   const [questionHistory, setQuestionHistory] = useState<string[]>([]);
   ```

2. **عند الرجوع، كان يفترض أن الحقل ينتمي للمدخل الأخير دائماً**:
   ```typescript
   const idx = list.length - 1;  // ⚠️ دائماً آخر مدخل!
   ```

3. **عند الرجوع من `education_more`، لم يكن يحذف المدخل الفارغ** الذي أُنشئ حديثاً.

#### ✅ الحل المطبق

1. **تحديث بنية الـ history** لتخزين `entryIndex`:
   ```typescript
   interface HistoryEntry {
       field: string;
       entryIndex?: number; // لتتبع أي مدخل في المصفوفة
   }
   const [questionHistory, setQuestionHistory] = useState<HistoryEntry[]>([]);
   ```

2. **تحديث `handleAnswer`** لحفظ `entryIndex` مع اسم الحقل للحقول المتكررة (education, experience, languages).

3. **تحديث `handleInternalBack`** لاستخدام `entryIndex` لتحديد المدخل الصحيح عند مسح البيانات.

4. **إضافة منطق حذف المدخلات الفارغة** عند الرجوع من `education_more`, `experience_more`, `languages_more`.

#### 📊 النتيجة
- TypeScript compilation: **passed** ✅
- الآن يمكن للمستخدم الرجوع بشكل صحيح بين المدخلات المتعددة

#### 📁 الملفات المعدلة
- [`src/components/wizard/QuestionnaireStep.tsx`](src/components/wizard/QuestionnaireStep.tsx)

---
