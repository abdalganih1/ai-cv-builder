# سجل المشاكل والحلول - AI CV Builder

> ملف توثيق لجميع المشاكل التي واجهها المشروع وكيف تم حلها، لمنع تكرارها.

---

## 📋 فهرس المشاكل

| # | المشكلة | القسم | الخطورة | الحالة |
|---|---------|-------|---------|--------|
| 1 | [Race condition عند الرجوع بالتنقل](#1-race-condition-عند-الرجوع-بالتنقل) | التنقل | 🔴 عالية | ✅ محلول |
| 2 | [getQuestionForField لا يدعم الحقول المصفوفية](#2-getquestionforfield-لا-يدعم-الحقول-المصفوفية) | التنقل | 🔴 عالية | ✅ محلول |
| 3 | [فقدان الموضع عند الخروج والعودة للاستبيان](#3-فقدان-الموضع-عند-الخروج-والعودة-للاستبيان) | التنقل | 🟡 متوسطة | ✅ محلول |
| 4 | [غياب checkpoints للتنقل المرئي](#4-غياب-checkpoints-للتنقل-المرئي) | واجهة | 🟡 متوسطة | ✅ محلول |

---

## التفاصيل

### 1. Race condition عند الرجوع بالتنقل

**العَرَض:** عند إدخال المواليد (birthDate) والضغط على "رجوع"، يتم تجاوز سؤال المواليد والعودة مباشرة لرقم الهاتف (خطوة Contact).

**السبب الجذري:**

- `handleInternalBack()` يستدعي `setIsRewinding(true)` (setState - تحديث غير فوري)
- ثم يستدعي `onUpdate(clearData)` الذي يُغيّر `data`
- تغيير `data` يُعيد تشغيل `useEffect` لجلب السؤال التالي
- `useEffect` يقرأ قيمة `isRewinding` من الـ closure القديمة (= `false`)
- فيتجاوز الحماية ويجلب السؤال التالي بدلاً من البقاء على السؤال المعيّن يدوياً

**الحل:**

```tsx
// استخدام useRef بجانب useState لضمان قراءة فورية
const isRewindingRef = useRef(false);
const setRewindingState = (val: boolean) => {
    isRewindingRef.current = val; // تحديث فوري (synchronous)
    setIsRewinding(val);         // تحديث React (async)
};

// في useEffect: فحص الـ ref أولاً (فوري) ثم الـ state
useEffect(() => {
    const fetchQuestion = async () => {
        if (isRewindingRef.current || isRewinding) return;
        // ...
        // فحص ثانٍ بعد العملية الـ async
        if (isRewindingRef.current) return;
        setCurrentQuestion(question);
    };
    fetchQuestion();
}, [data, isRewinding]);
```

**الملف المعدّل:** `src/components/wizard/QuestionnaireStep.tsx`
**تاريخ الإصلاح:** 2026-02-13

> ⚠️ **قاعدة عامة:** في أي مكان يتم فيه تعيين state يُستخدم كحماية في useEffect، يجب استخدام useRef بجانب useState لمنع race conditions.

---

### 2. getQuestionForField لا يدعم الحقول المصفوفية

**العَرَض:** عند الرجوع لسؤال في قسم التعليم أو الخبرات أو اللغات، لا يظهر السؤال ولا القيمة المخزنة.

**السبب الجذري:**

- الدالة القديمة `getQuestionForField()` في `QuestionnaireStep` كانت تدعم فقط الحقول الشخصية البسيطة
- لم يكن هناك دعم لحقول مثل `education_institution`، `experience_company`، `languages_name`
- هذه الحقول تحتاج `entryIndex` لمعرفة أي عنصر في المصفوفة يُشار إليه

**الحل:**

- إضافة `getQuestionForFieldDirect(field, data, entryIndex)` في `questionnaire-agent.ts`
- تدعم **جميع** أنواع الحقول مع السياق المناسب (مثل اسم الجامعة/الشركة)
- إضافة `populateResponseForField()` لاسترجاع القيم المخزنة لكل نوع حقل

**الملفات المعدّلة:**

- `src/lib/ai/questionnaire-agent.ts` (إضافة الدالة الجديدة)
- `src/components/wizard/QuestionnaireStep.tsx` (استخدام الدالة الجديدة)
**تاريخ الإصلاح:** 2026-02-13

> ⚠️ **قاعدة عامة:** أي دالة تتعامل مع حقول الاستبيان يجب أن تدعم جميع أنواع الحقول (شخصية + مصفوفية) وأن تمرر `entryIndex` عند الحاجة.

---

### 3. فقدان الموضع عند الخروج والعودة للاستبيان

**العَرَض:** عند الخروج من خطوة الاستبيان (رجوع لـ Contact) والعودة إليها، لا يمكن الرجوع للمدخلات القديمة.

**السبب الجذري:**

- عند إعادة تحميل المكون (`QuestionnaireStep`)، يبدأ بـ `questionHistory = []`
- `initializeHistoryFromData()` تبني History من البيانات المحفوظة
- لكنها لم تكن تشمل كل الحقول بشكل صحيح

**الحل:**

- إعادة كتابة `initializeHistoryFromData()` لتشمل جميع الحقول والمصفوفات
- تشغيلها مرة واحدة عند أول mount فقط
- بهذا يمكن للمستخدم الرجوع عبر كل الأسئلة المُجابة سابقاً

**الملف المعدّل:** `src/components/wizard/QuestionnaireStep.tsx`
**تاريخ الإصلاح:** 2026-02-13

---

### 4. غياب checkpoints للتنقل المرئي

**العَرَض:** لا يوجد طريقة مرئية للقفز بين أقسام الاستبيان (التعليم → الخبرات → إلخ).

**الحل:**

- إضافة شريط checkpoints أفقي قابل للنقر (6 أقسام: 👤🎓💼⚡🌍🎯)
- كل checkpoint له حالة: مكتمل (أخضر) / نشط (أزرق) / مقفل (رمادي)
- النقر على checkpoint مكتمل يأخذك لأول سؤال فيه
- `navigateToSection()` تُعيد بناء History وتنقلك مباشرة

**الملف المعدّل:** `src/components/wizard/QuestionnaireStep.tsx`
**تاريخ الإصلاح:** 2026-02-13

---

## 🔍 أنماط المشاكل المتكررة (يجب مراجعتها دائماً)

### نمط 1: Race Conditions مع useEffect

```
❌ المشكلة: setState(true) + onUpdate(data) → useEffect يقرأ القيمة القديمة
✅ الحل: استخدام useRef بجانب useState
```

### نمط 2: حقول مصفوفية بدون entryIndex

```
❌ المشكلة: التعامل مع education_institution بدون معرفة أي عنصر
✅ الحل: دائماً تتبع activeEntryIndex وتمريره مع كل عملية
```

### نمط 3: فقدان الحالة عند unmount/remount

```
❌ المشكلة: المكون يفقد state عند الخروج والعودة
✅ الحل: بناء الحالة من البيانات المحفوظة عند أول mount
```
